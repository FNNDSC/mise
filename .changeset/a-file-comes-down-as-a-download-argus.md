---
"@fnndsc/argus": patch
---

argus: a file comes down as a download. DOWNLOAD on a file row saved nothing a browser could show — it opened the bytes in a new tab. It now presses the daemon's attachment URL through the browser's own save gesture, and stands beside CLOSE on a text file's and an image's header too.
