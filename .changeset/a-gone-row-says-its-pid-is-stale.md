---
"@fnndsc/porter": patch
---

porter: `porter --status` marks the pid on a gone row as stale. The berth outlives a daemon killed with -9, and the bare number read as a process the listing had just made.
