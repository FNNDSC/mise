---
"@fnndsc/argus": patch
---

A pane header keeps its title on one line: a long name ellipsises (the whole of it on the tooltip) instead of wrapping inside the fixed-height bar, where it cut the letters and pushed the mode and state readouts onto a second row.
