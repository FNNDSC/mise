---
"@fnndsc/argus": patch
---

A volume a run produced opens from the node that made it. The dive inside a graph knew only raster images and read everything else as text, so a NIfTI in a node's `data` answered READ REFUSED; it now asks the same rule the browser does and opens an image pane beside the graph.
