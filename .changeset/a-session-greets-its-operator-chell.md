---
"@fnndsc/chell": patch
---

chell: the shell and the remote client greet through the kernel's `motd chell` where the fortune used to print, so a TTY and a browser on one session say the same thing. A session that cannot answer is left alone.
