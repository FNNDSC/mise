---
"@fnndsc/argus": minor
---

argus: the universe can fold across feeds. A VIEW block on the frame (and `universe view feeds|shapes`) switches the top between every feed as its own molecule (the default, the whole compute structure) and one molecule per pipeline shape, sized by the feeds folded under it and reddened by how many of them erred at each stage; a click on a folded shape unfolds it into its members in place. The scale block's second word is now ALIKE.
