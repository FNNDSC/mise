---
'@fnndsc/menu': minor
---

New `dicom.series` and `dicom.tags` model kinds: a folder as a series, and a file's tags or a folder's constant-versus-varying split, with identifying tags flagged so a surface can redact by the flag.
