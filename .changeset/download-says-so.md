---
"@fnndsc/argus": patch
---

DOWNLOAD saves the file or says why not. The page fetches the file with its own credentials and saves only an answer that is the file, under the name the daemon gives it (else the path's basename); a login page or a `not found` is no longer saved as a broken file named `vfs` — nothing is saved and the console says why.
