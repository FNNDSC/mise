---
"@fnndsc/argus": patch
---

argus: inside a feed in the universe, ENTER NODE (and a double click on a node, and `universe node <instance>`) flies into the node and opens its data inside it as a rooted browser; Esc flies out. It used to move the session's cwd, which said nothing on the field.
