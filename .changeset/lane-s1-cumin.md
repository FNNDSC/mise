---
"@fnndsc/cumin": minor
---

feat(cumin): the feed-load register learns failure — `feedLoad_fail` names where a walk stopped, `feedLoad_of` reads one feed's load, and a failed entry is forgotten after a minute
