---
"@fnndsc/menu": minor
---

feat(menu): the `feed.indexing` envelope model — a feed asked for while its topology is still being indexed — and a `failed` field on the prompt context's feed-load progress
