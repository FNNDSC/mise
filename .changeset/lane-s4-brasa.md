---
"@fnndsc/brasa": minor
---

feat(brasa): `proc feeds` and `proc jobs list` answer from the cache at once and start the roster sync off the session's lane; the prompt context names the walk while it runs
