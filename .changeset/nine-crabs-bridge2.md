---
"@fnndsc/chell": minor
---

Bridge second batch of printing builtins into envelopes (ls, tree, du, help, proc, logout, cubepath, query). Unbridge plugin: its add flow prompts for admin credentials through readline, which capture would make invisible; it stays a direct printer until the prompt capability lands with the engine facade.
