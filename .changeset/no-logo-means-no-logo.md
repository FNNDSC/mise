---
"@fnndsc/chell": patch
---

chell: `--no-logo` hides the logo. It used to print it anyway, uncoloured — the flag only chose the colour — so a boot read off a pipe carried a still brain above its rows. Off a TTY the flag is implied.
