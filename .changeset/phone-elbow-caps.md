---
"@fnndsc/argus": patch
---

On a phone the listing caps no longer run under the mode frame's elbow, whose black fillet cut a notch out of the PATIENT cap: caps clip at their own clear line. The PACS command line wraps, so EXPORT CSV is no longer cut off at the pane's edge.
