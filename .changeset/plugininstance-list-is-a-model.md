---
"@fnndsc/brasa": patch
---

`plugininstance list` answers with a `plugininstance.list` model beside its table: the listed instances as typed rows (id, plugin name and version, feed, owner, status, start), the fields the listing selected. A surface reads its run history rather than scraping it.
