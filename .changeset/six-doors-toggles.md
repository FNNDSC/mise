---
"@fnndsc/chell": minor
---

Convert timing, physicalmode, and debug builtins to envelope returns (typed models `sys.timing`, `sys.physicalMode`, `sys.debug`; rendered text byte-identical). Rename the sink channel methods to the project's RPN convention: `OutputSink.data_write` and `OutputSink.status_write`.
