---
"@fnndsc/argus": patch
---

The graph is the form. In a catalogue bound to an input, opening a plugin dives straight into its one node, and the dive's parameters are VALUE cells writing their flags into the run strip's line as typed (booleans bare, a hand edit standing). RUN rides the graph's frame. A pipeline node dives as `title · @id` editing `--<node>.<param>`; off a node it is refused by name, since the kernel runs a pipeline only on one.
