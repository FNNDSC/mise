---
"@fnndsc/argus": patch
---

argus: the UNIVERSE takes the stage — its own preset and primary pane, entered whole from the dashboard tile rather than split beside a browser — and its frame opens to the bar's width like every other field's, so the blocks stay on screen.
