---
"@fnndsc/argus": patch
---

argus: the file browser's way home is one press. A `~` row leads the listing above `..` anywhere but home, showing the home path beside it, and the path line above the listing is a trail whose every segment but the last goes there (starting at `~` under home, `/` elsewhere). Both move the browser exactly as a directory press does.
