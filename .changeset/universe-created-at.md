---
"@fnndsc/menu": minor
"@fnndsc/brasa": patch
---

`proc universe` says when each feed was made (`createdAt`, ISO 8601), the order a replay reveals the space in; a model from an older daemon reads it as empty.
