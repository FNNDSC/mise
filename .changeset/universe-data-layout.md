---
"@fnndsc/argus": minor
---

DATA on the universe's LAYOUT block (`universe layout data`): every feed hung from what it began from — a hub per format, then per DICOM modality, then per series-description group — each hub a captioned nebula sized by its feeds; a hub's tip reads its path and count, a press lights its feeds.
