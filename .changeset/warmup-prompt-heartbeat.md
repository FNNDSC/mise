---
'@fnndsc/calypso': patch
---

The telemetry heartbeat now refreshes the promptline while the process index moves: warm-up progress reaches idle surfaces without waiting for a command, and the prompt settles when reconciliation completes.
