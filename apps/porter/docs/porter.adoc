= PORTER: the mise display manager
Rudolph Pienaar <rudolph.pienaar@gmail.com>
:doctype: article
:toc: left
:toclevels: 3
:sectnums:
:icons: font

[abstract]
== Abstract

PORTER is to ARGUS what gdm is to a desktop: the thing that logs a human in, finds or starts their session, and shows them to the surface. It is not a surface (it attaches to nothing) and not the session (calypso is); it is the door. This document is its design log, kept the way `apps/argus/docs/aegis.adoc` keeps ARGUS's: every decision lands here in the pull request that makes it.

== Status

Grilled and settled 2026-09-21; built in slices from that day. Tier 2 of link:../../../docs/session-supervisor.adoc[the session supervisor] — the porter that document reserved a name for in 2026-07, arriving now that the web surface exists.

== Decisions

[cols="1,3"]
|===
| 2026-09-21 (the face says ChRIS) | The operator, on seeing PORTER across the top of the login page: "someone will just ask why doesn't this just say ChRIS login". Right. A person at the door is logging in to ChRIS; the name of the program at the door is this repository's business. The page's heading is ChRIS, its title "ChRIS · login", the greet "ChRIS · starting your session", and the states read as sentences to the person waiting ("your session is ready"). `porter` stays the package, the binary, the env prefix and the cookie — words only operators see. |
| 2026-09-21 (the greeter) | Slice 5. The operator's first login through the door hung the browser for a minute with nothing to look at: `POST /login` waited for the berth. Now the door answers at once. A session already up is entered directly; one that must boot sends the browser to `GET /greet/<key>`, where the brain wakes — `@fnndsc/menu/logo`'s frames, the same a terminal boot draws, paced by the page at 120 ms — and the daemon's boot rows arrive beneath it from `GET /boot/<key>` as they are written, ANSI and all, rendered by `@fnndsc/menu/ansi`, the converter the ARGUS console renders its transcript with (moved from argus to the wire package for the purpose; argus keeps only the DOM half). `ready` rests the brain and hands the browser to `/s/<key>/?door`; `failed` says why and offers the door again. The porter serves both modules from where menu is installed (`/greeter/brain.js`, `/greeter/ansi.js`) — nothing bundled, nothing copied: the console and the greeter draw from one source, which was the whole reason for putting the brain in menu. The registry learns a booting identity as PENDING and admits it to its mount only when the boot ends and the berth answers, so `/s/<key>/` refuses (404) until then and the greet is the only place a booting session can be looked at. The login page wears the same shell with the brain at rest. The cold boot itself is unchanged and is the cost of a fresh state directory — the first login per identity per host; every later one boots warm or attaches. |
| 2026-09-21 (the door itself) | Slice 4. `GET /login` is the plainest page that can ask for a name and a password, and the greeter will take its place without moving the route. `POST /login` takes the form or JSON; on a token it sets a SIGNED cookie naming the session's key — HttpOnly, SameSite=Lax, Secure when a front said the page came over TLS (`trustProxy`), a day long by default (`PORTER_COOKIE_HOURS`), the same span an idle session lives, since CUBE's tokens carry no expiry to follow — and sends the browser to `/s/<key>/?door`; a refusal sends the form back to the door with the reason in the query, and a JSON caller gets the status. The cookie gates `/boot/<key>`, `/s/<key>/…` and the upgrade: a browser reaches only the session its cookie names, a request with none is sent to the door (302), a boot ask without it is refused (401), an upgrade without it is dropped before a byte reaches the daemon. `POST /logout` clears the cookie and touches the session not at all — a daemon outlives every surface, eviction decides when it ends, and a LOG OUT that killed the session would make the next login a cold boot. The signing secret is `PORTER_SECRET`, at least twenty characters; given none the porter makes one up per start and says so, and every browser is asked again at a restart. The root `/` sends a browser let in to its session and anyone else to the door. In argus the LOG OUT pill stands only on a page that came through a door and leaves by it (`apps/argus/docs/aegis.adoc`, a-surface-leaves-by-the-door-it-came-in). With the gate in place the porter may listen beyond loopback, behind TLS. |
| 2026-09-21 (a porter at the door) | Slice 3, the shape without the door. `apps/porter` is an app, not a package, because it is what gdm is to GNOME Shell: its own program with its own face, sibling of the DE, importing calypso for berths and nothing of argus. Fastify, with `@fastify/reply-from` for the HTTP proxy and the porter's own upgrade relay for the wire — `@fastify/http-proxy` routes to ONE upstream and cannot rewrite an attach, and every identity is its own upstream. `POST /sessions` takes a username and a password, trades the password at CUBE's `auth-token/` for a token, and starts (or joins) that identity's session with the token on stdin — `chell --daemon <user>@<url> --auth-token-stdin` — in four directories of its own under the state dir, keyed by the berth key; the password is held for the length of one HTTP call and lands nowhere. The session is mounted at `/s/<key>/`, the key being calypso's berth key for the identity — sixteen hex characters, stable across porter restarts, carrying no URL of anyone's into the URL bar. Under the mount the page, its assets and `/vfs` are proxied to the daemon on loopback, the attach token put on the proxied `/vfs` query and the proxied upgrade URL by the porter (calypso reads it there since this slice); the browser holds no token and is sent to `…/?door`. `GET /boot/<key>` is the session's boot as server-sent events — one per line the daemon wrote, ANSI and all, replayed for a late attacher — ending in `ready` or `failed`: the greeter's feed, and the same text the terminal boot shows. Nothing checks a cookie yet, so a porter at this slice binds loopback; the door itself (login page, cookie, LOG OUT) is the next slice and gates `/s/` and `/boot/`. Every login re-checks the password at CUBE even when the session is up: a berth proves the daemon, not the human.
| 2026-09-21 (settled at the grill) | One CUBE per porter, from config, never a field. `SessionHost` seam: `ProcessHost` on titan first, `KubernetesHost` on argentum when titan has users. Cookie TTL = CUBE's if it has one, else 24 h, aligned with a 24 h idle eviction. TLS external — caddy or nginx on titan, an Ingress on argentum; the porter trusts `X-Forwarded-Proto` and never terminates. The greeter shows the SAME boot the terminal shows: the ASCII brain (`@fnndsc/menu/logo`, paced by the greeter) beside the boot rows off the daemon's stdout. No `--host-control` on titan, where every session shares one uid; it returns with pods, where a shell in one's own container is one's own. Fastify rather than FastAPI because the porter imports calypso's berth resolver, brasa's token connect and chell's entry — a Python front would shell out to every one of them.
|===

== Routes

[cols="1,3",options="header"]
|===
| Route | What it does
| `GET /healthz` | The CUBE this door serves and how many sessions it knows.
| `GET /` | To the session the cookie names (`/s/<key>/?door`), else to `/login`.
| `GET /login` | The door: the brain at rest, username and password.
| `POST /login` | Form or JSON `{username, password}`. Sets the cookie and answers at once. Form: 303 to `/s/<key>/?door` when the session is up, to `/greet/<key>` when it must boot, or back to `/login?reason=…`. JSON: `{key, mount, state}` (`attached` or `starting`); 401 with CUBE's word.
| `GET /greet/<key>` | Cookie-gated. The greeter: the brain awake, the boot rows arriving, the hand-off to the session on `ready`.
| `GET /greeter/brain.js`, `/greeter/ansi.js` | The wire package's own modules, served from where it is installed.
| `POST /logout` | Clears the cookie; the session lives on. Form: 303 to `/login`. JSON: `{ok: true}`.
| `GET /boot/<key>` | Cookie-gated (401). Server-sent events: `line` (`{channel, text}`), then `ready` or `failed` (`{reason}`).
| `/s/<key>/…` | Cookie-gated (302 to `/login`; an upgrade without it is dropped). The session: page, assets, `/vfs`, and the WebSocket wire, proxied to that identity's daemon on loopback with the token added by the porter.
|===

== Configuration

Environment only, read once at start.

[cols="1,3",options="header"]
|===
| Variable | Meaning
| `PORTER_CUBE_URL` | Required. The one CUBE this door serves, API base (a trailing slash is added).
| `PORTER_STATE_DIR` | Where each identity's `config`, `run`, `cache`, `tmp` live. Default `$XDG_STATE_HOME/porter`, else `~/.local/state/porter`.
| `PORTER_HOST`, `PORTER_PORT` | Bind address; default `127.0.0.1:4180`.
| `PORTER_CHELL` | The chell entry to start sessions with; default the `@fnndsc/chell` installed beside the porter.
| `PORTER_SECRET` | What the cookie is signed with, at least twenty characters. Given none, one is made up per start and every browser is asked again at a restart.
| `PORTER_COOKIE_HOURS` | How long a browser stays let in; default 24.
|===
