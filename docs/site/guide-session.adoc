= The session API: host or attach to a live session
:toc: left
:toclevels: 2
:source-highlighter: highlight.js
:example-root: ../../packages/chell/exemplars/ts

== What this is

The session API is the pairing of `brasa` (the engine: parser, dispatch, builtins, pipes, session state, output sinks, surface capabilities) and `calypso` (the daemon that hosts an engine behind a WebSocket, plus the zod-validated wire contract a client speaks); the packages install as `@fnndsc/brasa` and `@fnndsc/calypso`. A program on this API exchanges command lines for command envelopes: structured results carrying rendered text, error text and status, exactly what the `chell` shell itself runs on.

== Why this is

Some programs are not scripts but surfaces: a web terminal, a chat integration, an agent that operates ChRIS conversationally. Such a program needs a session (working directory, connection, caches, history), needs interactivity to happen on the correct machine (a password prompt or a pipe through `grep` must run where the human is, never on a daemon host), and needs results as data rather than scraped text. The engine plus the wire contract provide precisely that, and the capability inversion is the load-bearing design: prompts, pipeline segments, shell escapes and local edits are surface capabilities the engine requests, so an attached client executes them locally and returns the outcome.

== How this relates to existing session machinery

`chell`'s interactive REPL, `chell -c` one-shots, the `chell --daemon` host and `chell --remote` client are all thin arrangements of these same parts; nothing documented here is a side door. The forthcoming web surface is planned as another such arrangement. For the protocol itself, every message crossing the socket validates against the schemas in the link:api/modules/_fnndsc_calypso.html[`calypso` reference].

== The example is a program, and the program is a test

The example below is a complete, standalone program, included in full: it needs no CUBE and no credentials, and continuous integration runs it on every pull request. It lives in `packages/chell/exemplars/ts/standalone/`; copy it whole and it runs.

== Host, attach, execute

One program, both roles. It creates the real `brasa` engine, hosts it behind a real `calypso` daemon on an ephemeral port, attaches `chell`'s production `RemoteEngine` client over the WebSocket, executes one line, and reads the typed envelope back:

[source,typescript]
----
include::{example-root}/standalone/session_wire.ts[]
----

Each envelope carries `status`, `rendered` and optionally `renderedErr`, so a hosting program presents output, detects failure and sets exit codes without parsing text. In a real deployment the two halves live in different processes, usually different machines; exemplar `05_calypsoDaemon.ts` shows that operational layer, spawning `chell --daemon`, discovering it through its berth file, and surviving a restart with the session rehydrated.

== Interactivity crosses the wire too

The client above passed no capability callbacks, so it attached as a non-interactive surface. A real surface passes them: `onPrompt` answers password and confirmation prompts, `onPipe` runs pipeline segments (`help | grep session`) through the client machine's own tools, `onShell` runs `!` escapes locally, and `onEdit` opens the client's editor and returns the result. Exemplar `06_wireRoundtrip.ts`, run by continuous integration on every pull request, exercises each of those round trips with assertions; this excerpt shows the daemon half it drives:

[source,typescript]
----
include::{example-root}/06_wireRoundtrip.ts[tag=daemonHost]
----

== Where to go next

link:api/modules/_fnndsc_brasa.html[`brasa` reference] for the engine, sinks, surfaces and envelope delivery; link:api/modules/_fnndsc_calypso.html[`calypso` reference] for `CalypsoDaemon`, the request brokers and the message schemas.
