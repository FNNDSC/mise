{
  "name": "@fnndsc/tui",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for the ChELL stack — cumin, salsa, chili, chell. Each published independently to npm.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @fnndsc/cumin && npm run build -w @fnndsc/salsa && npm run build -w @fnndsc/chili && npm run build -w @fnndsc/chell",
    "test": "npm test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@yao-pkg/pkg": "6.20.0",
    "esbuild": "0.28.1"
  },
  "engines": {
    "node": ">=20.12"
  }
}
