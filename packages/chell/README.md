# Chell - The ChRIS Shell

`chell` is an interactive command-line shell for interacting with a *ChRIS* instance.

## Under Construction

This project is currently under construction. It will leverage the command libraries from `chili` and the business logic from `salsa` to provide an interactive user experience.
